<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="output.css">
    <title>airbnb</title>
</head>
<body>
    <header>
        <h1 class="text-red-500 font-bold">Welcome to Airbnb</h1>
    </header>

    <main>
        <h2>Available Homes are shown below: </h2>
        <ul>
            <% if (registeredHomes && registeredHomes.length > 0) { %>
            <% registeredHomes.forEach(home => { %>
            <li>
                <%= home.housename %>
            </li>
            <% }) %>
            <% } else { %>
            <li>No homes available at the moment.</li>
            <% } %>
        </ul>
        <a href="/host/add-home"> Add Home </a>
        <a href="/contact-us"> Contact Us </a>
    </main>
</body>
</html>